<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kogile.checklist.mapper.ChecklistMapper">

	<!-- 데이터 출력 select문 -->
	<select id="getList" resultType="kogile.checklist.domain.ChecklistVO">
	<![CDATA[
	select * from checklist where checklist_no > 0
	]]>
	</select>

	<!-- insert문 -->
	<insert id="insert">
		insert into checklist (checklist_no, check_title, p_no  )
		values (checklist_seq.nextval, #{check_title}, #{p_no})
	</insert>
	
	<insert id="insertSelectKey">
		<selectKey keyProperty="checklist_no" order="BEFORE"
		resultType="long">
		select checklist_seq.nextval from dual
		</selectKey>
		
		insert into checklist(checklist_no, check_title, p_no)
		values (#{checklist_no}, #{check_title}, 1)
		<!-- values (#{checklist_no}, #{check_title}, #{p_no}) -->
	</insert>
	
	<select id="read" resultType="kogile.checklist.domain.ChecklistVO">
		select * from checklist where checklist_no = #{checklist_no}
	</select>
	
	<delete id="delete">
		delete checklist where checklist_no = #{checklist_no}
	</delete>
	
	<update id="update">
		update checklist 
		set check_title = #{check_title},
	    p_no = #{p_no}
	    where checklist_no = #{checklist_no}
	</update>
	
</mapper>
