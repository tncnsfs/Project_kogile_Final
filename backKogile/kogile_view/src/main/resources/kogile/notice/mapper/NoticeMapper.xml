<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kogile.notice.mapper.NoticeMapper">
	<select id="notice"   resultType="kogile.notice.domain.NoticeVO">
	 	select ntc_cont, to_char(ntc_date, 'YYYY-MM-DD HH:MI:SS') as day 
	 	from notice
	 	<where>
	 		total_m_no = #{total_m_no}
	 	</where>
	</select>
	
	<insert id="insertNotice" parameterType="kogile.notice.domain.NoticeVO">
		insert into notice(notice_no, invite_no, ntc_cont, ntc_date, total_m_no) values(notice_seq.nextval, #{invite_no}, '초대됐음!', sysdate, #{total_m_no})
	</insert>
	
</mapper>